import 'package:flutter/material.dart';

class GetFundingScreen extends StatelessWidget {
  const GetFundingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Get Funding')),
    );
  }
}
