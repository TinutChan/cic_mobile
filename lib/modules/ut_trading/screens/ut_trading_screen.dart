import 'package:flutter/material.dart';

class UTTradingScreen extends StatelessWidget {
  const UTTradingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('UT Trading')),
    );
  }
}
