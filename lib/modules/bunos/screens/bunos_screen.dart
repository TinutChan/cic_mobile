import 'package:flutter/material.dart';

class BunosScreen extends StatelessWidget {
  const BunosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Bunos')),
    );
  }
}